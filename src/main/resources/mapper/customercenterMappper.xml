<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devcamp.eztour.dao.customercenterMapper.">
<!--    Inquiry 매퍼-->
    <select id="countCustomerInquiry" resultType="int">
        SELECT count(*) FROM qna
    </select>

    <delete id="deleteAllCustomerInquiry">
        DELETE FROM qna
    </delete>

    <delete id="deleteCustomerInquiry" parameterType="map">
        DELETE FROM qna WHERE qna_no = #{qna_no} and usr_id = #{usr_id}
    </delete>

    <insert id="insertCustomerInquiry" parameterType="CustomerInquiryDto">
        INSERT INTO qna (usr_id, qna_cd, email, phn, qna_ttl, qna_cont)
        VALUES (#{usr_id}, #{qna_cd}, #{email}, #{phn}, #{qna_ttl}, #{qna_cont})
    </insert>

    <select id="selectAllCustomerInquiry" resultType="CustomerInquiryDto">
        SELECT *
        FROM qna
        ORDER BY frs_reg_date DESC, qna_no DESC
    </select>

    <select id="selectCustomerInquiry" parameterType="int" resultType="CustomerInquiryDto">
        SELECT *
        FROM qna
        WHERE qna_no = #{qna_no}
    </select>

    <select id="selectCustomerPage" parameterType="map" resultType="CustomerInquiryDto">
        SELECT *
        FROM board
        ORDER BY frs_reg_date DESC, qna_no DESC
            LIMIT #{offset}, #{pageSize}
    </select>

    <update id="updateCustomerInquiry" parameterType="CustomerInquiryDto">
        UPDATE qna
        SET   qna_ttl = #{qna_ttl}
          , qna_cont = #{qna_cont}
          , up_date = now()
          , fnl_mod_date = #{fnl_mod_date}
        WHERE qna_no = #{qna_no}
    </update>

<!--    조회수 매퍼-->
    <update id="increaseViewCntCustomerInquiry" parameterType="int">
        UPDATE qna
        SET   view_cnt = view_cnt + 1
        WHERE qna_no = #{qna_no}
    </update>

<!--    검색 기능 매퍼 시작-->
    <select id="searchSelectCustomerPage" parameterType="CustomerSearchCondition" resultType="CustomerInquiryDto">
        SELECT * FROM qna
        WHERE true
        <choose>
            <when test='option=="T"'>
                AND title LIKE concat('%', #{keyword}, '%')
            </when>
            <when test='option=="W"'>
                AND writer LIKE concat('%', #{keyword}, '%')
            </when>
            <otherwise>
                AND (title LIKE concat('%', #{keyword}, '%')
                OR content LIKE concat('%', #{keyword}, '%'))
            </otherwise>
        </choose>
        ORDER BY frs_reg_date DESC,qna_no DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="searchResultCntCustomerPage" parameterType="CustomerSearchCondition" resultType="int">
        SELECT count(*)
        FROM qna
        WHERE true
        <choose>
            <when test='option=="T"'>
                AND title LIKE concat('%', #{keyword}, '%')
            </when>
            <when test='option=="W"'>
                AND writer LIKE concat('%', #{keyword}, '%')
            </when>
            <otherwise>
                AND (title LIKE concat('%', #{keyword}, '%')
                OR content LIKE concat('%', #{keyword}, '%'))
            </otherwise>
        </choose>
    </select>
<!--    검색 기능 매퍼 끝-->
<!--inquiry 매퍼 끝-->
</mapper>